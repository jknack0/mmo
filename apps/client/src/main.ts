// Placeholder. Real client lands in S03 (#5): GatewayClient + ChannelClient +
// SnapshotInterpolator + IsoRenderer + InputController.

const statusEl = document.getElementById('status');
if (statusEl) {
  statusEl.innerHTML =
    'Alpha vertical slice — placeholder.<br>' +
    'Implementation tracked at <a href="https://github.com/jknack0/mmo/issues/5" target="_blank">issue #5</a>.';
}
