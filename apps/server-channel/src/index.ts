// Placeholder. Real Channel implementation lands in S03 (#5): ZoneState +
// 20Hz TickLoop + MovementSystem + SnapshotEncoder. Combat in S05 (#7).

console.log('[channel] placeholder — implementation tracked at https://github.com/jknack0/mmo/issues/5');

// Keep the process alive so `pnpm dev` composes.
setInterval(() => {}, 1 << 30);
